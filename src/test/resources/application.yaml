spring:
  messages:
    basename: i18n/appmessages
    encoding: UTF-8
  shell:
    interactive:
      enabled: false

questions:
  file: /questions
  success-count-percent: 40

application:
  locale: en
